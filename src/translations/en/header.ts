interface Word {
  [key: string]: string;
};

interface Word {
  home: string;
  addUser: string;
  usersList: string;
};

export const header: Word = {
  home: "home",
  addUser: "add User",
  usersList: "users list",
};


