interface Word {
  [key: string]: string;
};

interface Word {
  home: string;
  addUser: string;
  userList: string;
};

export const header: Word = {
  home: "home",
  addUser: "add User",
  userList: "users list",
};


